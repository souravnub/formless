export type AddRadioButtonProps = {
    title: string;
    radioButtons: string[];
};

export type AddCheckboxesProps = {
    title: string;
    checkboxes: string[];
};

export type addTextInputProps = {
    title: string;
    required: boolean;
    defaultVal: string;
    isMutableList: boolean;
};
export type addDecisionFieldsProps = {
    title: string;
    fields: string[];
};

export type addDecisionFieldsWithCommentProps = {
    title: string;
    fields: string[];
};
